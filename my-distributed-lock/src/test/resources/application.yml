# server
server:
  port: 8081
  tomcat:
    uri-encoding: UTF-8
# distributed-lock
my:
  distributed-lock:
    lock-type: "database"

spring:
  # 数据源
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/upms?useSSL=false&useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true
    username: root
    password: 123456

# logging
logging:
  level:
    com.zhuang.distributedlock.mapper: debug
